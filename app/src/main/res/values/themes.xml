<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base do sistema (janela/splash). A UI real é toda estilizada
         via MaterialTheme no Compose (Copa2002Theme). -->
    <style name="Theme.Copa2002" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">#FF121212</item>
        <item name="android:navigationBarColor">#FF121212</item>
    </style>
</resources>
